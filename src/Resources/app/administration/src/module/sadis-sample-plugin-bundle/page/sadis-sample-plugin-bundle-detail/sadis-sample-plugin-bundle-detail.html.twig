{% block sadis_sample_plugin_bundle_detail %}
    <sw-page class="sadis-sample-plugin-bundle-detail">
        <template slot="smart-bar-actions">
            <sw-button :routerLink="{ name: 'sadis-sample-plugin.bundle.list' }">
                {{ $t('sadis-sample-plugin-bundle.detail.cancelButtonText') }}
            </sw-button>

            <sw-button-process
                :isLoading="isLoading"
                :processSuccess="processSuccess"
                variant="primary"
                @process-finish="saveFinish"
                @click="onClickSave">
                {{ $t('sadis-sample-plugin-bundle.detail.saveButtonText') }}
            </sw-button-process>
        </template>

        <template slot="content">
            <sw-card-view>
                <sw-card v-if="bundle" :isLoading="isLoading">
                    <sw-field :label="$t('sadis-sample-plugin-bundle.detail.nameLabel')" v-model="bundle.name"></sw-field>
                    <sw-field :label="$t('sadis-sample-plugin-bundle.detail.discountLabel')" v-model="bundle.discount" type="number"></sw-field>

                    <sw-field type="radio"
                              :label="$t('sadis-sample-plugin-bundle.detail.discountTypeLabel')"
                              v-model="bundle.discountType"
                              :options="options">
                    </sw-field>

                    <sw-entity-many-to-many-select
                        :localMode="bundle.isNew()"
                        :label="$t('sadis-sample-plugin-bundle.detail.assignProductsLabel')"
                        v-model="bundle.products">
                    </sw-entity-many-to-many-select>
                </sw-card>
            </sw-card-view>
        </template>
    </sw-page>
{% endblock %}
